{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-xs-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ redirect.slug }}</h5>
                    <p class="card-text">{{ redirect.url }}</p>
                    <a href="/{{ redirect.slug }}" class="btn btn-primary">Go to link</a>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
